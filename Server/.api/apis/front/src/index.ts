import SDK from './sdk.js';

/**
 * Core API
 *
 * @author Front Platform
 * @see {@link https://community.front.com}
 */
const createSDK = (() => { return new SDK(); })();

export default createSDK;
